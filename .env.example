# Turismo Cómbita - Variables de Entorno
# Copiar a .env y ajustar valores para producción

PORT=3001
NODE_ENV=production

# Seguridad - OBLIGATORIO cambiar en produccion
JWT_SECRET=CAMBIAR_POR_UN_SECRET_SEGURO_EN_PRODUCCION

# Usuario admin inicial (solo se usa si no existe users.json)
ADMIN_EMAIL=admin@tudominio.com
ADMIN_PASSWORD=CAMBIAR_CONTRASENA_SEGURA

# CORS - origenes permitidos (separados por coma)
CORS_ORIGINS=https://tudominio.com
